# 多系统知识库使用指南

## 概述

Sakura AI 现已支持**多系统知识库隔离架构**。每个系统拥有独立的向量数据库集合，避免跨系统知识污染，提高 AI 生成测试用例的准确性。

## 架构设计

### 集合命名规则

```
test_knowledge_{系统名称}
```

例如:
- `test_knowledge_实物2.0` - 实物2.0系统的知识库
- `test_knowledge_渠道集采` - 渠道集采系统的知识库
- `test_knowledge_拍卖` - 拍卖系统的知识库

### 系统组件

1. **向量数据库**: Qdrant (`http://localhost:6333`)
2. **后端服务**:
   - [testCaseKnowledgeBase.ts](../server/services/testCaseKnowledgeBase.ts) - 知识库核心服务
   - [knowledgeManagementService.ts](../server/services/knowledgeManagementService.ts) - 知识管理业务逻辑
   - [knowledge.ts](../server/routes/knowledge.ts) - 知识库 API 路由
   - [systems.ts](../server/routes/systems.ts) - 系统管理扩展 API
3. **AI 集成**: [functionalTestCaseAIService.ts](../server/services/functionalTestCaseAIService.ts) - 自动使用系统特定知识库

## 快速开始

### 1. 查看知识库状态

```bash
npm run knowledge:status
```

输出示例:
```
📊 Sakura AI 知识库状态总览
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗄️  Qdrant 向量数据库地址: http://localhost:6333
📚 知识库集合总数: 3

┌─ 系统: 实物2.0
├─ 集合名称: test_knowledge_实物2.0
├─ 知识点数量: 20 条
├─ 向量维度: 1024D
├─ 距离计算: Cosine
└─ 状态: green
```

### 2. 为新系统创建知识库集合

#### 方式一: 通过 API (推荐)

```bash
# 获取系统 ID
curl http://localhost:3001/api/v1/systems

# 为系统创建知识库集合
curl -X POST "http://localhost:3001/api/v1/systems/{系统ID}/knowledge-collection" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### 方式二: 直接操作 Qdrant

```bash
# 使用 URL 编码的集合名称
curl -X PUT "http://localhost:6333/collections/test_knowledge_%E6%96%B0%E7%B3%BB%E7%BB%9F" \
  -H "Content-Type: application/json" \
  -d '{"vectors": {"size": 1536, "distance": "Cosine"}}'
```

### 3. 添加知识到系统知识库

#### 单条添加

```bash
curl -X POST "http://localhost:3001/api/v1/knowledge/实物2.0/add" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "category": "business_rule",
    "title": "订单超时自动取消规则",
    "content": "未支付订单超过30分钟自动取消，库存回滚，用户收到取消通知",
    "businessDomain": "订单管理",
    "tags": ["订单", "超时", "自动取消"]
  }'
```

#### 批量导入

```bash
curl -X POST "http://localhost:3001/api/v1/knowledge/实物2.0/batch-import" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "knowledgeList": [
      {
        "category": "test_pattern",
        "title": "优惠叠加测试模式",
        "content": "测试多个优惠同时使用时的计算逻辑...",
        "businessDomain": "营销促销",
        "tags": ["优惠", "叠加"]
      },
      {
        "category": "pitfall",
        "title": "并发库存扣减问题",
        "content": "高并发场景下库存扣减可能出现超卖...",
        "businessDomain": "库存管理",
        "tags": ["并发", "库存", "超卖"]
      }
    ]
  }'
```

#### JSON 文件导入

```bash
curl -X POST "http://localhost:3001/api/v1/knowledge/实物2.0/import-json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@knowledge.json"
```

JSON 文件格式:
```json
[
  {
    "category": "business_rule",
    "title": "业务规则标题",
    "content": "详细内容描述",
    "businessDomain": "业务领域",
    "tags": ["标签1", "标签2"]
  }
]
```

### 4. 搜索知识

```bash
# 在指定系统中搜索
curl "http://localhost:3001/api/v1/knowledge/search?query=订单超时&systemName=实物2.0&topK=5" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 按类别搜索
curl "http://localhost:3001/api/v1/knowledge/实物2.0/search-by-category?query=订单&topK=3" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## API 参考

### 知识库管理 API

#### 基础操作

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/v1/knowledge/collections` | 列出所有知识库集合 |
| GET | `/api/v1/knowledge/stats` | 获取所有系统统计 |
| GET | `/api/v1/knowledge/:systemName/stats` | 获取指定系统统计 |

#### 知识操作

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/api/v1/knowledge/:systemName/add` | 添加单条知识 |
| POST | `/api/v1/knowledge/:systemName/batch-import` | 批量导入知识 |
| POST | `/api/v1/knowledge/:systemName/import-json` | 从 JSON 文件导入 |
| DELETE | `/api/v1/knowledge/:systemName/clear` | 清空系统知识库 |

#### 搜索操作

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/api/v1/knowledge/search` | 搜索知识 |
| POST | `/api/v1/knowledge/test-search` | 测试搜索功能 |
| GET | `/api/v1/knowledge/:systemName/search-by-category` | 按类别搜索 |

### 系统管理扩展 API

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/api/v1/systems/:id/knowledge-collection` | 为系统创建知识库集合 |
| GET | `/api/v1/systems/:id/knowledge-collection` | 获取系统知识库统计 |
| DELETE | `/api/v1/systems/:id/knowledge-collection` | 删除系统知识库集合 |

## 知识类别

系统支持 4 种知识类别:

### 1. business_rule (业务规则)

业务流程、规则、约束条件

**示例**:
- 订单超时自动取消规则
- 用户等级权益说明
- 退款流程规则

### 2. test_pattern (测试模式)

测试方法、测试策略、常见测试场景

**示例**:
- 优惠叠加测试模式
- 边界值测试方法
- 并发压力测试场景

### 3. pitfall (历史踩坑点)

过往发现的 bug、容易出错的地方

**示例**:
- 并发库存扣减超卖问题
- 金额计算精度丢失
- 时区转换错误

### 4. risk_scenario (资损风险场景)

可能导致资金损失的高风险场景

**示例**:
- 优惠券重复使用漏洞
- 价格负数漏洞
- 积分无限刷取漏洞

## AI 测试生成集成

### 自动使用系统知识库

当创建功能测试用例时，AI 会**自动**使用对应系统的知识库进行 RAG (检索增强生成):

```typescript
// 在 functionalTestCaseAIService.ts 中
const knowledgeBase = this.getKnowledgeBase(systemName);
const knowledgeResults = await knowledgeBase.searchByCategory({
  query: queryText,
  topK: 3,
  scoreThreshold: 0.5
});
```

### 知识库增强效果

- ✅ 自动引用业务规则生成测试点
- ✅ 基于历史踩坑点生成边界测试
- ✅ 根据风险场景生成资损测试
- ✅ 应用测试模式生成最佳实践用例

### 查看 RAG 日志

服务器启动后，查看日志可以看到知识库检索过程:

```
📚 [知识库RAG] 开始检索相关知识...
   🎯 目标系统: 实物2.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 [RAG-Step1] 准备查询参数:
   📌 测试目的: "订单创建流程"
   📌 检索参数: topK=3, scoreThreshold=0.5
✅ [RAG-Step2] 向量检索完成 (耗时: 45ms)
📊 [RAG-Step3] 知识检索结果汇总:
   ✅ 业务规则: 2条
   ✅ 测试模式: 1条
   ✅ 历史踩坑点: 1条
```

## 数据迁移

### 从旧集合迁移数据

如果已有 `test_knowledge` 集合需要迁移到新的系统特定集合:

```bash
npm run knowledge:migrate
```

该脚本会:
1. 读取源集合所有知识点
2. 复制到目标集合
3. 验证迁移结果

### 清理无效集合

```bash
npm run knowledge:clean
```

删除所有不在白名单中的集合。

## 故障排查

### 问题 1: 集合未创建

**症状**: 向量数据库中看不到对应的集合

**解决方案**:
1. 检查 Qdrant 是否运行: `curl http://localhost:6333/collections`
2. 手动创建集合 (见上文"为新系统创建知识库集合")
3. 检查系统名称是否正确

### 问题 2: AI 未使用知识库

**症状**: 生成的测试用例没有引用知识库内容

**可能原因**:
- 知识库为空 (添加知识)
- 知识与测试内容相似度过低 (调整 `scoreThreshold`)
- 系统名称不匹配

**检查方法**:
```bash
# 查看系统知识库统计
curl "http://localhost:3001/api/v1/knowledge/实物2.0/stats" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 测试搜索
curl "http://localhost:3001/api/v1/knowledge/search?query=测试内容&systemName=实物2.0" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 问题 3: 向量维度不匹配

**症状**: 插入知识时报错 "vector dimension mismatch"

**原因**: 不同 embedding 模型的向量维度不同
- Google Gemini: 768D
- OpenAI text-embedding-3-small: 1536D
- OpenAI text-embedding-ada-002: 1536D

**解决方案**: 创建集合时指定正确的维度，或重新创建集合

## 最佳实践

### 1. 知识组织

- 每个系统至少包含 10-20 条核心业务规则
- 记录所有历史 bug 作为踩坑点
- 总结常用测试模式供 AI 参考
- 高风险场景必须完整记录

### 2. 知识质量

- 标题简洁明确 (10-30 字)
- 内容详细具体 (100-500 字)
- 标签精准 (3-5 个)
- 定期更新维护

### 3. 性能优化

- 使用批量导入而非单条添加
- 控制每个集合大小 (建议 < 10000 条)
- 定期清理过时知识

### 4. 安全建议

- 不要在知识库中存储敏感信息 (密码、密钥等)
- 定期备份知识库数据
- 控制 API 访问权限

## 后续开发计划

### 前端管理界面 (待开发)

- [ ] 知识库管理页面
  - 知识列表展示
  - 搜索和过滤
  - 添加/编辑/删除知识
- [ ] 批量导入界面
  - Excel 文件上传
  - JSON 文件上传
  - 导入进度显示
- [ ] 测试搜索工具
  - 实时测试知识检索
  - 查看相似度评分
- [ ] 系统管理扩展
  - 在系统列表添加"管理知识库"按钮
  - 显示知识库统计信息

## 相关文件

### 后端

- [server/services/testCaseKnowledgeBase.ts](../server/services/testCaseKnowledgeBase.ts) - 知识库核心服务
- [server/services/knowledgeManagementService.ts](../server/services/knowledgeManagementService.ts) - 知识管理业务逻辑
- [server/services/functionalTestCaseAIService.ts](../server/services/functionalTestCaseAIService.ts) - AI 测试生成集成
- [server/routes/knowledge.ts](../server/routes/knowledge.ts) - 知识库 API
- [server/routes/systems.ts](../server/routes/systems.ts) - 系统管理 API

### 脚本

- [scripts/showKnowledgeStatus.mjs](../scripts/showKnowledgeStatus.mjs) - 查看知识库状态
- [scripts/migrateKnowledge.mjs](../scripts/migrateKnowledge.mjs) - 迁移知识库数据
- [scripts/cleanCollections.mjs](../scripts/cleanCollections.mjs) - 清理无效集合

### 数据库

- Qdrant 向量数据库: `http://localhost:6333`
- Dashboard: `http://localhost:6333/dashboard`

## 技术支持

如有问题，请查看:
1. 服务器日志: `d:\AI_mvp\ai_test\project\logs\server.log`
2. Qdrant Dashboard: `http://localhost:6333/dashboard`
3. API 文档: 本文档 API 参考章节
